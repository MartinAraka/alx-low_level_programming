#include <stdio.h>
/**
 * main - main function of loop.
 *
 * Return: 0 always when successful
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
