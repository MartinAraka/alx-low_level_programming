#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @i: parameter
 * @j: parameter
 *
 * Return: sum
 */

int add(int i, int j)
{
	int sum = (i + j);

	return (sum);
}
